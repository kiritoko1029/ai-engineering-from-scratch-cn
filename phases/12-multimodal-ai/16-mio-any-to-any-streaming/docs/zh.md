# MIO 与任意到任意流式多模态模型

> GPT-4o 交付了一个大多数开放模型无法复制的产品形态：一个能听语音、看视频并实时语音回应的智能体。到 2024 年底，开放生态的答案是 MIO（Wang 等人，2024 年 9 月）。MIO 对文本、图像、语音和音乐进行 token 化，在交错序列上训练一个因果 Transformer，实现任意模态到任意模态的生成。AnyGPT（Zhan 等人，2024 年 2 月）是概念验证；MIO 是规模化版本；Unified-IO 2（Allen AI，2023 年 12 月）是具有视觉 + 动作基础能力的近亲。本课将解读任意到任意模式——四个 tokenizer、一个 Transformer、流式友好的解码。

**类型：** 学习
**语言：** Python（标准库，四模态 token 分配器 + 流式解码循环）
**前置要求：** 第 12 阶段 · 第 11 课（Chameleon），第 6 阶段（语音与音频）
**所需时间：** 约120分钟

## 学习目标

- 设计一个共享词汇表，容纳文本、图像、语音和音乐 token 而不产生冲突。
- 比较 SEED-Tokenizer（图像）和 SpeechTokenizer 残差 VQ（语音）在压缩与重建之间的权衡。
- 解释四阶段课程如何逐步构建任意到任意生成能力。
- 列出三种开放的任意到任意方案及其主要权衡：MIO、AnyGPT、Unified-IO 2。

## 问题所在

统一多模态模型说起来容易，规模化构建却很难。2024 年之前的大多数"任意到任意"系统都是流水线式的：视觉模型 → 文本表示 → 语音模型 → 音频。每一步跳转都会丢失信息、增加延迟、使训练复杂化。GPT-4o 的演示视频展示了一个单模型方案，具有亚秒级响应；开放系统落后了数月。

工程挑战：

- 每种模态都必须有 tokenizer，压缩到足够无损以支持重建，并以 Transformer 能消费的速率产生 token。
- 单一词汇表必须为文本（32k+）、图像（16k+）、语音（4k+）、音乐（8k+）分配空间。最低需要四万多个条目。
- 训练数据必须覆盖每种输入输出组合（文本→图像、图像→语音、语音→图像等），或者模型必须具备组合能力。
- 推理时必须以足够快的速度流式输出 token，以满足对话延迟要求（<500ms 首音频字节时间）。

## 概念说明

### 四种模态的四个 tokenizer

MIO 的 tokenizer 栈：

- 文本：标准 BPE，词汇量约 32000。
- 图像：SEED-Tokenizer（2023）——带有离散码本的量化 VAE，4096 个条目，每张图像 32x32 个 token。
- 语音：SpeechTokenizer 残差 VQ（2023）——将 16kHz 波形编码为 8 层层级码本；第一层是粗粒度内容，后续层添加韵律和说话人身份信息。
- 音乐：类似的残差 VQ（Meta 的 MusicGen / Encodec 系列），4-8 个码本。

每种模态产生整数 token。这些 token 在共享词汇表中获得不相交的 ID 范围：

```
text:   0..31999
image:  32000..36095  (4096 image tokens)
speech: 36096..40191  (4096 speech base tokens, plus residual layers)
music:  40192..48383  (8192 music tokens)
sep:    48384..48390  (<image>, <speech>, <music>, </...>, etc.)
```

总计：约 48k 词汇量。输入嵌入和输出投影覆盖全部范围。

### 流式解码

语音生成使用残差 VQ。Transformer 预测基础层（第 0 层）的语音 token；并行解码的残差量化器预测后续层。每个第 0 层 token 在 16kHz 下大约对应 50ms 的音频。

流式模式：

1. 用户对着麦克风说话；实时音频 tokenizer 每 50ms 发出语音 token。
2. MIO 在 token 到达时消费（提示预填充 + 增量前向传播）。
3. 输出 token 在生成时流式输出；并行语音解码器以约 50-150ms 的延迟将它们转换为音频样本。
4. 首音频字节时间：MIO 论文中约 300-500ms，接近 GPT-4o 的约 250ms。

Mini-Omni（arXiv:2408.16725）、GLM-4-Voice（arXiv:2412.02612）和 Moshi（arXiv:2410.00037）是互补的流式语音 LLM 设计。Moshi 尤其在单 GPU 上实现了 160ms 的往返延迟。

### 四阶段课程

MIO 的训练课程：

1. 第一阶段——对齐。大规模模态对语料库：文本-图像、文本-语音、文本-音乐。每对使用自己的 token 词汇表段。训练共享词汇表。
2. 第二阶段——交错。多模态交错文档（带图像 + 视频的博客、带转录文本的播客等）。训练跨模态上下文。
3. 第三阶段——语音增强。额外的音频数据以提升语音质量，同时不丢失文本能力。
4. 第四阶段——SFT。跨模态指令调优：VQA、描述、旁白、语音到语音对话。

缺少某个阶段会降低特定能力：跳过第二阶段，模型失去跨模态上下文；跳过第三阶段，语音质量变差。

### 视觉思维链

MIO 引入了视觉思维链：模型将中间图像 token 作为推理步骤输出。对于"猫在爬树吗？"这个问题，模型：

1. 输出渲染场景的 `<image>` token（来自输入图像或草图）。
2. 输出分析草图的文本。
3. 输出最终答案。

渲染的中间图像充当了草稿纸。在空间推理任务上，基准测试成绩有所提升。这个思路与文本推理的思维链类似。

### 任意到任意的竞争者

- AnyGPT（arXiv:2402.12226）：4 种模态（文本、图像、语音、音乐），类似设计。
- Unified-IO 2（arXiv:2312.17172）：增加了视觉动作输出、深度、法线。任务多样性更高，规模较小。
- NExT-GPT（arXiv:2309.05519）：LLM + 模态特定的扩散解码器。不是单模型方案。
- CoDi（arXiv:2305.11846）：可组合扩散；通过共享潜在空间实现任意到任意。

MIO 是最接近纯 token 任意到任意的方案。AnyGPT 是其概念先驱。

### 延迟预算

对于对话产品，每个组件的延迟都很重要：

- 麦克风到音频 token：约 50ms。
- 预填充（音频 token + 历史）：8B 模型上约 100ms。
- 第一个输出 token：约 50ms。
- 并行残差 VQ + 语音解码：约 100-150ms。

总首音频字节时间：最低约 300ms。GPT-4o 声称约 250ms。Moshi 声称 160ms。MIO/AnyGPT 根据公开基准测试在 400-600ms 范围。

### 为什么任意到任意仍然困难

即使到 2026 年，开放的任意到任意模型在两个轴上仍然落后于闭源模型：

- 语音质量。残差 VQ tokenizer 是有损的；对话式语音与 ElevenLabs 级别的声音相比听起来机械。
- 跨模态推理。让模型"唱出你看到的内容"仍然比纯视觉任务更容易失败。

这些都是开放的研究问题。Qwen3-Omni（第 12.20 课）是 2025 年最先进的开放尝试。

## 开始构建

`code/main.py`：

- 定义四模态词汇表分配并打印。
- 将多模态输入列表（文本、图像、音频片段、音乐）通过 tokenizer 路由器。
- 模拟文本到语音响应的流式解码，并统计延迟。
- 给定编码器、预填充和解码器延迟，计算预期的首音频字节时间。

## 交付成果

本课生成 `outputs/skill-any-to-any-pipeline-auditor.md`。给定一个对话产品规格（输入模态、输出模态、延迟目标），审计 MIO 系列的设计选择并计算延迟预算。

## 练习

1. 你的产品接受语音输入并返回语音输出。端到端延迟预算目标是多少？列出消耗时间的组件。

2. SpeechTokenizer 残差 VQ 使用 8 个码本。解释为什么并行解码残差层是必要的（vs 顺序解码），以及它带来了多少延迟节省。

3. 你的词汇表有 32k 文本 + 4k 图像 + 4k 语音。增加 8k 音乐和约 10 个分隔符。在隐藏维度 4096 下，嵌入矩阵的参数成本是多少？

4. 视觉思维链输出中间图像。哪些类型的问题受益？哪些类型的问题会因额外 token 而受损？

5. 阅读 Moshi（arXiv:2410.00037）。描述其"内心独白"技术，并与 MIO 的视觉思维链进行比较。

## 关键术语

| 术语 | 通俗说法 | 实际含义 |
|------|----------|----------|
| 任意到任意 | "多模态输入/输出" | 单个模型能接受和输出文本、图像、语音和音乐，任意方向 |
| 残差 VQ | "语音 tokenizer 栈" | 多码本 token 化，每层添加信息；基础层是内容，后续层是韵律 |
| SEED-Tokenizer | "图像编码" | MIO 使用的离散图像 tokenizer，具有 4096 条目码本 |
| 视觉思维链 | "视觉草稿纸" | 模型在最终答案之前生成中间图像作为推理步骤 |
| 首音频字节时间 | "TTFAB" | 从用户语音到首个音频输出的延迟；<500ms 才有对话感 |
| 四阶段课程 | "训练方案" | 对齐 → 交错 → 语音增强 → SFT，按此顺序 |

## 延伸阅读

- [Wang 等人 — MIO (arXiv:2409.17692)](https://arxiv.org/abs/2409.17692)
- [Zhan 等人 — AnyGPT (arXiv:2402.12226)](https://arxiv.org/abs/2402.12226)
- [Lu 等人 — Unified-IO 2 (arXiv:2312.17172)](https://arxiv.org/abs/2312.17172)
- [Wu 等人 — NExT-GPT (arXiv:2309.05519)](https://arxiv.org/abs/2309.05519)
- [Tang 等人 — CoDi (arXiv:2305.11846)](https://arxiv.org/abs/2305.11846)
