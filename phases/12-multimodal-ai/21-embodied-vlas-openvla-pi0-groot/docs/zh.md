# 具身 VLA：RT-2、OpenVLA、π0、GR00T

> 模型第一次从网站上读取菜谱并在厨房机器人上执行，是 RT-2（Google DeepMind，2023 年 7 月）。RT-2 将动作离散化为文本 token，在网络数据和机器人动作数据上联合微调 VLM，证明了网络规模的视觉-语言知识可以迁移到机器人控制。OpenVLA（2024 年 6 月）发布了开放的 7B 参考实现。Physical Intelligence 的 π0 系列（2024-2025）加入了流匹配动作专家。NVIDIA 的 GR00T N1（2025 年 3 月）为类人机器人提供了大规模的双系统（System 1 / System 2）控制。VLA 原语——视觉-语言-动作，一个能看、能读、能行动的单一模型——是本阶段理解模型与第 15 阶段自主系统之间的桥梁。

**类型：** 学习
**语言：** Python（标准库，动作 tokenizer + VLA 推理骨架）
**前置要求：** 第 12 阶段 · 第 05 课（LLaVA），第 15 阶段（自主系统，参考引用）
**所需时间：** 约180分钟

## 学习目标

- 描述动作 token 化：离散分箱编码（RT-2）、FAST 高效动作 token、连续流匹配动作（π0）。
- 解释为什么在 web + 机器人数据上联合微调能保留通用知识到新任务的迁移。
- 在同一机器人任务上比较 OpenVLA（开放 7B Llama+VLM）、π0（流匹配）和 GR00T N1（双系统）。
- 列出 Open X-Embodiment 数据集及其作为 RT-X 训练语料库的角色。

## 问题所在

用自然语言指令做家务的机器人自 1970 年代以来就是研究目标。2020 年代的答案：视觉-语言-动作（VLA）模型。与 VQA 使用相同的 VLM 架构，但输出是动作（关节扭矩、末端执行器位姿、离散命令）而非文本。

VLA 特有的挑战：

1. 动作空间是连续的（关节角度、力）且高维的（7 自由度臂 + 3 自由度夹爪 = 10 维，30 Hz）。
2. 机器人特定的训练数据稀缺。Open X-Embodiment 约有 100 万条轨迹；网络文本-图像有 50 亿+。
3. 控制频率很重要。30 Hz 控制循环意味着每个动作 33ms 的预算。
4. 安全性。错误的动作会损坏硬件、伤害人类或破坏财产。

## 概念说明

### 动作 token 化（RT-2）

RT-2 的技巧：将每个关节目标表示为量化的文本 token。将归一化的 [-1, 1] 范围离散化为 256 个分箱，将每个分箱映射到词汇表 ID。一个 10 自由度的动作在每个控制步变成 10 个 token。

在混合数据上联合微调 PaLM-X VLM：

- 网络图文对（描述、VQA）。
- 机器人演示，动作以 token 形式表示。

模型看到"捡起红色方块"（语言）→ 图像（视觉）→ 10 个 token 的动作序列（离散化的关节目标）。网络预训练保留了通用知识迁移：RT-2 可以遵循"移向快速移动的物体"，即使"快速移动"不在训练数据中。

RT-2 论文中推理速度 3-5 Hz，受限于 VLM 自回归解码。

### OpenVLA——开放的 7B 参考实现

OpenVLA（Kim 等人，2024 年 6 月）是开放权重的 RT-2 等价物。7B Llama 骨干，DINOv2 + SigLIP 双视觉编码器，256 分箱的动作 token 化。

在 Open X-Embodiment（97 万条轨迹，22 种机器人）上训练。附带 LoRA 微调支持以适配新机器人。

推理：在 A100 上量化后 4-5 Hz。对慢速操控足够快，对高频控制不够。

### FAST tokenizer——更快的动作解码

Pertsch 等人（2024）表明离散分箱 token 化效率低下——大多数动作集中在分箱空间的小区域内。FAST（频域动作序列 tokenizer）通过 DCT 压缩动作序列并量化系数。

一条 30 步动作轨迹变成约 10 个 FAST token，而非 300 个离散分箱 token。推理速度提升 3-5 倍，质量无损。

### π0 和流匹配动作

Physical Intelligence 的 π0（Black 等人，2024 年 10 月）用流匹配动作专家替代离散动作 token：

- 一个小型动作 Transformer 读取 VLM 的隐藏状态，通过整流流输出连续的 50 步动作序列。
- 动作头用流匹配损失训练；VLM 预训练保持不变。
- 推理：约 5 个去噪步输出完整动作序列，有效 50 Hz 控制。

π0 的主张：在广泛的操控任务套件上超越 OpenVLA 和 Octo。连续动作公式保留了离散化所破坏的平滑性。

π0.5 和 π0-FAST 是增量升级。π0-FAST 将 FAST token 化与流匹配结合。

### GR00T N1——类人机器人的双系统

NVIDIA 的 GR00T N1（2025 年 3 月）为类人机器人构建（>30 自自由度，全身）：

- System 2：大型 VLM 阅读场景 + 指令，以约 1 Hz 产生高层子目标。
- System 1：小型动作头 Transformer 以 50-100 Hz 产生低层关节命令，以子目标为条件。

这种拆分映射到 Kahneman 的快慢思维：System 2 规划，System 1 行动。好处：慢速的 VLM 级规划不会阻塞快速控制；System 1 保持小型以降低延迟。

GR00T N1.7（2025 年底）改进了数据扩展。GR00T 使用来自 Omniverse 的仿真到真实数据进行微调。

### Open X-Embodiment

训练数据。RT-X（2023 年 10 月）汇集了 22 个数据集，覆盖 22 种机器人的 100 万条轨迹。Open X-Embodiment 是所有人都在使用的语料库：

- ALOHA / Bridge V2 / Droid / RT-2 Kitchen / Language Table。
- 每个样本：（机器人状态、摄像头视角、指令、动作序列）。
- 训练卫生：统一动作空间、归一化关节范围、调整摄像头。

OpenVLA 和 π0 在 Open X-Embodiment 上训练。到任何特定机器人的领域差距通过 100-1000 个任务特定演示的 LoRA 微调来弥合。

### 联合微调 vs 仅机器人数据

联合微调将网络 VQA 数据与机器人轨迹混合。比例很重要：太多 VQA 模型会遗忘动作；太多机器人数据模型会失去通用知识。

RT-2 的比例：约 1:1。OpenVLA：约 0.5:1 网络对机器人。π0：类似。精确比例是需要根据数据集规模调优的超参数。

仅机器人训练产生任务特定模型，在分布外指令上失败。联合微调是"捡起红色方块（演示中的）"和"从左边数第三大的物体（新表述）"之间的区别。

### 安全与动作限制

每个生产 VLA 都附带：

- 硬关节限制（不能超过规格的扭矩）。
- 速度限制（软裁剪）。
- 工作空间边界（末端执行器不能离开桌面）。
- 新任务的人工审批。

这些位于 VLA 外部，作为控制层检查。VLA 的输出是建议，不是命令。

## 开始构建

`code/main.py`：

- 实现 256 分箱动作 token 化和反 token 化。
- 基于 DCT + 量化构建 FAST tokenizer 骨架。
- 比较（离散分箱、FAST、连续流）每步动作的 token 数量。
- 打印 RT-2 → OpenVLA → π0 → GR00T 的谱系摘要。

## 交付成果

本课生成 `outputs/skill-vla-action-format-picker.md`。给定一个机器人任务（操控、导航、类人全身），在离散分箱 + RT-2、FAST + OpenVLA、流匹配 + π0 或双系统 + GR00T 之间做出选择。

## 练习

1. 一个 10 自由度臂，30 Hz 控制频率。256 分箱的离散分箱 token 化每秒输出多少个 token？7B VLM 能跟上吗？

2. FAST token 化将 30 步轨迹压缩到约 10 个 token。如果轨迹包含高频运动（如打鼓），用户会损失什么？

3. π0 的流匹配头在约 5 步内去噪。与 OpenVLA 4-5 Hz 的自回归解码比较吞吐量。

4. GR00T 的 System 1 / System 2 拆分映射到 Kahneman。提出一种不同的拆分（System 3？）可能有助于双足行走。

5. 阅读 Open X-Embodiment 第 4 节关于数据集整理的内容。列出防止领域泄漏的三条整理规则。

## 关键术语

| 术语 | 通俗说法 | 实际含义 |
|------|----------|----------|
| VLA | "视觉-语言-动作" | 接受图像 + 指令并输出动作命令的模型 |
| 动作 token 化 | "离散分箱" | 将连续关节目标量化为每维 256 个分箱，每个分箱对应一个词汇表 ID |
| FAST tokenizer | "频域动作 token" | DCT + 量化将 30 步轨迹压缩到约 10 个 token |
| 联合微调 | "混合网络 + 机器人" | 在网络 VQA 数据和机器人演示上同时训练以保留通用知识 |
| 流匹配动作头 | "π0 连续输出" | 小型 Transformer 通过整流流输出 50 步动作序列 |
| System 1 / System 2 | "双系统控制" | 大型 VLM 慢速规划，小型动作头快速行动；GR00T 模式 |
| Open X-Embodiment | "RT-X 数据集" | 100 万条轨迹的跨机器人数据集；训练语料库 |

## 延伸阅读

- [Brohan 等人 — RT-2 (arXiv:2307.15818)](https://arxiv.org/abs/2307.15818)
- [Kim 等人 — OpenVLA (arXiv:2406.09246)](https://arxiv.org/abs/2406.09246)
- [Black 等人 — π0 (arXiv:2410.24164)](https://arxiv.org/abs/2410.24164)
- [NVIDIA — GR00T N1 (arXiv:2503.14734)](https://arxiv.org/abs/2503.14734)
- [Open X-Embodiment Collab — RT-X (arXiv:2310.08864)](https://arxiv.org/abs/2310.08864)
