# 翻译规范（en.md → zh.md）

将课程文档从英文翻译为简体中文。每个 `docs/en.md` 翻译为同目录下的 `docs/zh.md`。

## 总原则
- 面向中文 AI 工程学习者，译文要**自然流畅、专业准确**，不要生硬的机翻腔。
- 保持原文的 Markdown 结构、标题层级、列表、表格、顺序完全一致。

## 必须翻译
- 所有正文段落、标题（`#`/`##`/`###`）、列表项、表格内的说明文字。
- 引用块（`> ...`）中的正文。
- 加粗的元信息标签及其值，例如：
  - `**Type:** Build` → `**类型：** 构建`
  - `**Languages:**` → `**语言：**`
  - `**Prerequisites:** None` → `**前置要求：** 无`
  - `**Time:** ~45 minutes` → `**所需时间：** 约45分钟`
- 常见标题对照（参考，按语义灵活处理）：
  - Learning Objectives → 学习目标
  - The Problem → 问题所在
  - The Concept → 概念说明
  - Build It → 开始构建
  - Step N → 第 N 步
  - Exercises / Practice → 练习
  - Recap / Summary → 小结 / 总结
  - Key Takeaways → 关键要点
  - Further Reading / References → 延伸阅读 / 参考资料

## 保持原文不译
- **代码块整体不译**（```...``` 之间的内容），包括代码内的注释（如 `# Install PyTorch`）保持英文原样。
- mermaid / graph 图表代码块内的标签保持原文。
- 数学公式（LaTeX，`$...$`、`$$...$$`）。
- 行内代码 `like_this`、函数名、类名、库名、文件名、命令、URL、路径。
- 专有名词/技术术语首次出现可用「中文（English）」形式，但常见缩写（GPU、CUDA、API、LLM、Transformer、PyTorch 等）直接保留英文。

## 标点与排版
- 正文使用中文全角标点：，。：；、「」（）？！
- 中英文之间、中文与数字之间无需强制加空格，但术语前后可酌情加半角空格以提升可读性（参考已有译文风格）。
- 冒号用全角「：」，例如 `**类型：** 构建`。

## 输出
- 直接写入同目录的 `zh.md`，不要修改 `en.md`。
- 不要添加译者注、不要增删章节。
